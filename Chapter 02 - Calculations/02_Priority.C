#include <stdio.h>

int main()
{
    // int a = 9+3*5-5;
    int a = 9/3*27-5;
    printf("The value of a is %d", a);
    // operator associativity :
    // x*y/z = (x*y)/z
    // x/y*z = (x/y)*z
    // (left to right)
    return 0;
}