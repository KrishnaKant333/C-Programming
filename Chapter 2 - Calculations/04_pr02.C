#include<stdio.h>

int main(){
    
    int a;
    printf("Enter the number which will be checked, if its divisible by 97\n");
    scanf("%d", &a);
    printf("The remainder is %d", a%97);
    return 0;
}